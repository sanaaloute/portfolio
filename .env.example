# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=portfolio

# Backend auth
JWT_SECRET=change-me-to-a-long-random-string-min-32-characters
ADMIN_PASSWORD=your-secure-admin-password
# For production, generate a bcrypt hash and use ADMIN_PASSWORD_HASH instead:
# ADMIN_PASSWORD_HASH=$2b$12$...
# Auth cookie — set COOKIE_SECURE=true in production (requires HTTPS)
COOKIE_SECURE=false

# CORS — for prod compose use http://localhost:8080, otherwise set to your frontend origin
CORS_ORIGINS=http://localhost:8080
